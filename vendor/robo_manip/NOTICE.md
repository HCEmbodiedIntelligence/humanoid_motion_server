# Internal Binary SDK Notice

`humanoid_motion_server/vendor/robo_manip` contains a binary SDK snapshot supplied for internal integration with `humanoid_motion_control`.

- Included assets are limited to the delivered public headers and `librobo_manip.so`, `libmotion_control.so`, and `liblibplaco.so`.
- The original algorithm is maintained by other colleagues in a separate repository. This project neither owns nor reimplements that source code.
- SDK version and source repository commit are unknown for this delivery.
- Redistribution authorization is pending confirmation. Do not publish, mirror, sublicense, or distribute these assets outside the approved environment until permission is documented.
- No open-source license is asserted or created for these SDK assets.

Provenance and integrity details are recorded in `MANIFEST.yaml` and `SHA256SUMS`.
